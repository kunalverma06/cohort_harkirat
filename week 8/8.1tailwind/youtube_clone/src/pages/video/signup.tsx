import Form from "../../components/Signup"; // or adjust the path if different

export default function SignupPage() {
  return (
    <div className="flex justify-center items-center min-h-screen">
      <Form />
    </div>
  );
}
